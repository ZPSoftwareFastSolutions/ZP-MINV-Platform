<Window x:Class="MINV.DesktopClient.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:c="clr-namespace:MINV.DesktopClient.Controls"
        xmlns:vm="clr-namespace:MINV.DesktopClient.ViewModels"
        Title="M-INV" Width="620" Height="420" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Icon="/Assets/minv.ico"
        FontFamily="{StaticResource UiFont}" Opacity="0">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.35" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
    <Border Margin="24" CornerRadius="20" Background="{DynamicResource BrandGradient}" Effect="{StaticResource PopupShadow}">
        <Grid ClipToBounds="True">
            <!-- Formas decorativas -->
            <Ellipse Width="360" Height="360" Fill="#FFFFFF" Opacity="0.06" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-170,-140,0" />
            <Ellipse Width="220" Height="220" Fill="#FFFFFF" Opacity="0.05" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-90,0,0,-120" />
            <Canvas HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="190" Height="120" Margin="0,0,34,70" Opacity="0.16">
                <Rectangle Canvas.Left="0" Canvas.Top="70" Width="22" Height="50" RadiusX="5" RadiusY="5" Fill="#FFFFFF" />
                <Rectangle Canvas.Left="34" Canvas.Top="40" Width="22" Height="80" RadiusX="5" RadiusY="5" Fill="#FFFFFF" />
                <Rectangle Canvas.Left="68" Canvas.Top="58" Width="22" Height="62" RadiusX="5" RadiusY="5" Fill="#FFFFFF" />
                <Rectangle Canvas.Left="102" Canvas.Top="18" Width="22" Height="102" RadiusX="5" RadiusY="5" Fill="#FFFFFF" />
                <Rectangle Canvas.Left="136" Canvas.Top="0" Width="22" Height="120" RadiusX="5" RadiusY="5" Fill="#FFFFFF" />
            </Canvas>

            <Grid Margin="40,34,40,28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Border Width="54" Height="54" CornerRadius="14" Background="#26FFFFFF">
                        <Image Source="/Assets/minv-256.png" Width="40" Height="40" RenderOptions.BitmapScalingMode="HighQuality" />
                    </Border>
                    <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="M-INV" Foreground="#FFFFFF" FontSize="30" FontWeight="Bold" />
                        <TextBlock Text="Inventario inteligente para su empresa" Foreground="#D6E6FF" FontSize="13" />
                    </StackPanel>
                </StackPanel>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Steps}" Margin="2,30,0,0" VerticalAlignment="Center" Focusable="False">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:StartupStep}">
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <Grid Width="20" Height="20">
                                    <Ellipse x:Name="Pending" Width="14" Height="14" Stroke="#9FC3F5" StrokeThickness="1.5" />
                                    <c:ProgressRing x:Name="Running" Width="16" Height="16" Foreground="#FFFFFF" Visibility="Collapsed" />
                                    <TextBlock x:Name="Done" Style="{StaticResource Icon}" Text="{x:Static c:Glyphs.CheckCircle}" FontSize="17"
                                               Foreground="#7EE2A8" Visibility="Collapsed" />
                                    <TextBlock x:Name="Warn" Style="{StaticResource Icon}" Text="{x:Static c:Glyphs.Warning}" FontSize="15"
                                               Foreground="#FCD34D" Visibility="Collapsed" />
                                </Grid>
                                <TextBlock x:Name="Label" Text="{Binding Text}" Foreground="#B7D2F7" FontSize="13.5" Margin="12,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding State}" Value="Running">
                                    <Setter TargetName="Pending" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="Running" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="Label" Property="Foreground" Value="#FFFFFF" />
                                    <Setter TargetName="Label" Property="FontWeight" Value="SemiBold" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding State}" Value="Done">
                                    <Setter TargetName="Pending" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="Done" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="Label" Property="Foreground" Value="#E6F0FF" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding State}" Value="Warning">
                                    <Setter TargetName="Pending" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="Warn" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="Label" Property="Foreground" Value="#FDE68A" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Grid.Row="2" Margin="0,18,0,0">
                    <TextBlock Text="{Binding Status}" Foreground="#FFFFFF" FontSize="12.5" Margin="0,0,0,8" />
                    <ProgressBar x:Name="Bar" Minimum="0" Maximum="100" Height="5" Foreground="#FFFFFF" Background="#33FFFFFF" />
                </StackPanel>
                <DockPanel Grid.Row="3" Margin="0,16,0,0">
                    <TextBlock DockPanel.Dock="Right" Text="{Binding Version}" Foreground="#9FC3F5" FontSize="11" />
                    <TextBlock Text="© Z&amp;P Software Fast Solutions" Foreground="#9FC3F5" FontSize="11" />
                </DockPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
