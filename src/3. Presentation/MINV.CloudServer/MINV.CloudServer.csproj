<Project Sdk="Microsoft.NET.Sdk.Web">
  <!-- V4 · Servidor en la nube del cliente de escritorio: el escritorio no tiene credenciales de la base de datos; envía
       sus comandos de MediatR por HTTPS y este servidor los ejecuta con la tubería completa (validación, permisos y
       alcance por sucursal calculados en el servidor, auditoría, idempotencia). -->
  <PropertyGroup>
    <RootNamespace>MINV.CloudServer</RootNamespace>
    <AssemblyName>MINV.CloudServer</AssemblyName>
    <AssemblyTitle>M-INV · Servidor en la nube</AssemblyTitle>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\2. Infrastructure\MINV.Infrastructure\MINV.Infrastructure.csproj" />
  </ItemGroup>
</Project>
